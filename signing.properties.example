# Copy to .signing/signing.properties, or pass -PreleaseSigningProperties=/absolute/path/to/config.
# All paths are relative to the project root unless absolute.
storeFile=.signing/release.p12
storePassword=REPLACE_WITH_YOUR_PRIVATE_PASSWORD
keyAlias=electronic-tadpole
keyPassword=REPLACE_WITH_YOUR_PRIVATE_PASSWORD
